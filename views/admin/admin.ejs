<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin Page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@500&display=swap" rel="stylesheet">
     



  <style>
    body {
    background-color: #f2f6fc;
    margin: 0;
    padding: 0; /* Removed padding here */
  }
    body {
      background: #DAD7CD;
      /* font-family: 'Segoe UI', sans-serif; */
       font-family: 'Baloo 2', cursive, sans-serif;
    }
  
    .navbar{
      background-color: #3A5A40;
      height: 90px;
      font-size: 1.4rem;
    }
     .navbar-brand
     {
      font-size: 50px;
      font-weight: Bolder;
      font-family: 'Baloo 2', cursive, sans-serif;
     }
    .navbar-brand, .nav-link, .btn-light {
      color: white !important;
     
    }
    .nav-item
    {
      margin-left: 20px;
    }
    h1 {
      text-align: center;
      color: #2c3e50;
      margin-bottom: 40px;
    }

    .product-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }

    .product-card {
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
      width: 300px;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .product-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    }

    .product-content {
      padding: 20px;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .product-title {
      font-size: 20px;
      font-weight: 600;
      color: #333;
    }

    .product-desc, .info-row {
      font-size: 14px;
      color: #555;
    }

    .info-row strong {
      color: #2c3e50;
    }

    .price {
      font-size: 16px;
      font-weight: bold;
      color: #27ae60;
    }

    .button-group {
      margin-top: 15px;
      display: flex;
      justify-content: space-between;
    }

    .btn {
      padding: 8px 14px;
      font-size: 14px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    }

    .btn-edit {
      background-color: #3498db;
      color: white;
    }

    .btn-remove {
      background-color: #e74c3c;
      color: white;
    }

    .btn-edit:hover {
      background-color: #2980b9;
    }

    .btn-remove:hover {
      background-color: #c0392b;
    }
     .hero {
  background: url('/image/aahar.jpg') center/cover no-repeat;
  height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  color: #588157;
  text-align: center;
}

    .hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,0.4);
      z-index: 0;
    }
   

    .hero-content {
  position: relative;
  background: rgba(255, 255, 255, 0.2); 
  padding: 2rem 3rem;
  border-radius: 1rem;
  z-index: 1;
  max-width: 600px;
  backdrop-filter: blur(0.7px);      
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.3); 
}

    .hero-content h1 {
      font-size: 3rem;
      /* color: #588157; */
      color: white;
    }
    .hero-content p {
      font-size: 1.3rem;
      /* color: #344E41; */
      color: white;
    }
  </style>
</head>
<body>

  <!-- Full Width Navbar -->
<nav class="navbar navbar-expand-lg">
    <div class="container">
      <a class="navbar-brand" href="/index">आहार</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon" style="filter: invert(1)"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/admin/product">Products</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/users">Users</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/addProduct">Add Product</a></li>
          <li class="nav-item"><a class="nav-link" href="/admin/contactDetails">Contacters</a></li>
          <li class="nav-item"><a class="nav-link" href="/index">P/C</a></li>
        </ul>
      
      </div>
    </div>
  </nav>


<!-- Hero Section -->
  <section class="hero">
    <div class="hero-content">
      <h1>आहार</h1>
      <!-- <p>Bringing Farmers Closer to Your Table</p> -->
      <p>उत्पादक र उपभोक्तालाई जोड्ने स्वच्छ पुल।</p>
    </div>
  </section>

</body>
</html>
